
import 'search_screen_item_model.dart';

/// This class defines the variables used in the [search_screen_page],
/// and is typically used to hold data that is passed between different parts of the application.
class SearchScreenModel {
  // Rx<List<SearchScreenItemModel>> searchScreenItemList =
  //     Rx(List.generate(6, (index) => SearchScreenItemModel()));


  static List<SearchScreenItemModel> searchScreenItemList(){
   return [

   ];
  }
}
